<!-- Ajax script here? or use cloudinary?-->
<div class="uk-container">
    <div class="uk-column-1-3">
        <!-- profile picture section -->
        <section id="profilePictureSec">
            <div id="profile-sec">
                <h2 class="uk-heading-medium"></h2>
            </div>
            <!-- src should be the pic user adds -->
            <img src="" id="photo" alt="usersPhoto">
            <input id="picFile" type="file" name="profile_pic" placeholder="Picture" required="" capture>
            <label for="picFile" id="upload">Click to Upload or Drop Profile Picture</label>
        </section>
        <!-- artist bio section -->
        <section id="artistBioSec">
            <div class="bioSection">
                <h2 class="uk-heading-medium" id="artistHeader">{{ Artist }} </h2>
                <ul class="uk-iconnav-vertical">
        {{!-- connect buttons to user's socials --}}
                    <li><a href="#" class="uk-icon-button uk-margin-small-right" uk-icon="icon: instagram"></a></li>
                    <li><a href="#" class="uk-icon-button uk-margin-small-right" uk-icon="icon: twitter"></a></li>
                    <li><a href="#" class="uk-icon-button uk-margin-small-right" uk-icon="icon: facebook"></a></li>
                </ul>
                <!-- email button? or contact button for user to contact artist -->
            </div>
            <div>
                <h3 class="aboutMe">About me</h3>
                <p>""</p>
            </div>
  </section>
        <!--container for gallery-->
        <section>

        </section>
{{!--  --}}
    </div>
    <section>

    </section>
</div>